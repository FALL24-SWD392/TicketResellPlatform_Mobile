module.exports = {
    plugins: [
      require('tailwindcss')(),
      require('autoprefixer')(),
        someAsyncPlugin.process(css).then(result => {
            // Do something with the result
            }
        )
    ]
  };
  